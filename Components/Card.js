import styled from "@emotion/native";

export const Card = styled.View({
  flex: 1,
  shadowColor: "#000",
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.2,
  shadowRadius: 2,
  elevation: 5,
  backgroundColor: "#fff",
  borderRadius: 5,
  marginBottom: 10,
  padding: 20,
  marginHorizontal: 20,
  width: "auto",
  alignItems: "stretch",
});
